# Magna Park Self Store — auto-poster credentials
# Copy this file to .env and fill in the real values.
# .env is gitignored — NEVER commit it (the MAGNA-PARK repo is public).

# Facebook Page access token for "Magna Park Self Store Ltd"
# (Page ID 1012907521897286 — hardcoded in auto-poster.py).
# Use a permanent (never-expiring) Page token from Meta Business Suite.
META_PAGE_ACCESS_TOKEN=

# OpenAI API key — generates the post content.
OPENAI_API_KEY=
